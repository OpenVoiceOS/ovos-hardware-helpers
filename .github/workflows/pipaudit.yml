name: Pip Audit

on:
  push:
    branches: [master, dev]
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  pip_audit:
    uses: OpenVoiceOS/gh-automations/.github/workflows/pip-audit.yml@dev
    secrets: inherit
    with:
      ignore_vulns: 'GHSA-r9hx-vwmv-q579'
